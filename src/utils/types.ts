export type ProductCardProps = {
  product: {
    name: string
    img: string
    price: string
    id: number
  }
}

export type ProductProps = {
  author: string
  description: string
  evaluation: string
  name: string
  img: string
  price: string
  id: number
  stock: number
}
